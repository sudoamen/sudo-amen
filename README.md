# sudo-amen

Affirmations as code.  
Embedding care into the systems we build.

`sudo(amen)` is a tiny, dependency-free CLI and utility library that adds intentionality to development workflows.

It is chain-agnostic.  
Framework-agnostic.  
Optional.  

Designed for builders who care about how software shapes the world.

## Install

```bash
npm install --save-dev sudo-amen
